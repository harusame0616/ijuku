@agent-docs/チケット管理.md 86exchjt9 のチケットの作成を行います。まずは内容を整理します。現在は nextjs のデフォルト設定のみです。現在はモノレポですが、js プロジェクトは現在 web だけなので一旦ものレポでは作らず、 web に設定を導入します。ベースのプロジェクトは neostandard を使いたいと考えています。 これは flat config に対応していてある程度知名度があるパッケージだからです。他に代替のパッケージがないか調べて欲しいです。また本プロジェクトでは vitest,tailwindcss を入れているのでこれらのlint 設定も入れたいです。他にも使っているパッケージなどで lint が用意されていれば導入したいので、他にどのような lint 設定を入れるべきかの調査が必要です

---

各パッケージのルールを一覧し解説してください

---

a11y と prettier の対応も必要です

---

eslint はスタイルのフォーマットを行わないのがベストプラクティスというか推奨だった気がするんですが、どっちがいいですか？

---

eslint-config-prettier を導入する

---

tailwind css のクラスのソートはどっちでやるべき？ ast が変わるものは eslint?