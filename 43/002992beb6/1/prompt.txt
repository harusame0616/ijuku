go の標準の http サーバーって middleware 機能ある？

---

enum はない？

---

DB　で A -> B -> C  のような親子関係がある時に正規化すると Aの ID は B が持って B の ID は C がもつような形にするけど、アプリケーションで利用する際に C に対応する A の情報を撮る時に C -> B -> A と JOIN する必要があるよね。こういう場合は非正規化しておくべなの？