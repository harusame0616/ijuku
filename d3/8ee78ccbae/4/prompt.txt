@.github/workflows/api-test.yml で除外フィルターの条件が十分じゃありません。除外すべきファイルを調査して paths-filter の条件を修正して

---

いっている意味がわからない。今回のテストは apps/api のテストで apps/api に関係があるファイルの変更が会った時のい実行が必要です。
every を設定しない場合は or 条件になるため
apps/api/ 配下のファイルに変更が会った場合
apps/api/**/*.md 以外のファイルが変更された場合
apps/api/sqlc.yaml 以外のファイルが変更された場合
などそれぞれの条件に一致してしまます。
本来の意図は
apps/api 配下のファイルが変更された 勝つ *.md ファイル以外かつ ... といった形の and 条件が正しい認識です
それでいうと
compose.yml については別のフィルター条件に切り出す必要があります

---

進めて

---

毎回 steps.filter.outputs.api == 'true' || steps.... == 'true' と書くのが冗長なので結果を１つの変数にまとめるステップを挟むことは可能ですか？

---

github_output ってどういうしよう？ dorny/paths-filter みたいに 複数の結果を含めるのはどうすればいいの？

---

じゃあ例えばキーバリューと単一の値が含まれてしまっている場合はどうなるの

KEY1=test
KEY2=test
foo

---

なるほど result= で代入していたね