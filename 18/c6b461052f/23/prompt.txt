@apps/api/migrations/000001_sample.up.sql をレビューして

---

error: Dirty database version 1. Fix and force version.

---

error: migration failed: syntax error at or near "_created_at" (column 5) in line 32: CREATE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW._updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;


CREATE EXTENSION ltree;

CREATE TABLE "categories" (
    category_id UUID PRIMARY KEY,
    name TEXT NOT NULL,
    path ltree NOT NULL,

    _created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    _updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE "courses" (
    course_id UUID PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    slug TEXT NOT NULL,
    tags JSONB NOT NULL,
    publish_status TEXT NOT NULL,
    category_id UUID NOT NULL REFERENCES categories(category_id),
    published_at TIMESTAMPTZ,
    last_updated_at TIMESTAMPTZ

    _created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    _updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
);

CREATE TRIGGER trigger_courses_updated_at
    BEFORE UPDATE ON courses
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at();

COMMENT ON COLUMN courses.publish_status IS 'draft / published / archived';
 (details: pq: syntax error at or near "_created_at")